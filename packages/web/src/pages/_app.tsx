import App from 'next/app';
import { Header } from '@Organisms';
import '../styles/global.scss';

export default App;

<Header
  logo="nevskii"
  logoMobile="K"
  onCreateAccount={() => null}
  onLogin={() => null}
  onLogout={() => null}
/>;
